apiVersion: v1
kind: Service
metadata:
  name: forfrontend-demo
  labels:
    app: demo-voting-app
spec:
  selector:
    name: frontend-demo-app
  app: demo-voting-app
  type: NodePort  
  ports:
      - port: 80
        targetPort: 80
        nodePort: 30002